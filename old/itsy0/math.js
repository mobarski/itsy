sin = Math.sin
cos = Math.cos
atan2 = Math.atan2
exp = Math.exp
log = Math.log
pi = Math.PI

sign = Math.sign
abs = Math.abs
max = Math.max
min = Math.min
function mid(x,y,z) {
	return [x,y,z].sort( (a,b)=>a-b )[1]
}

ceil = Math.ceil
floor = Math.floor

function rnd(x) {
	return Math.random()*x
}
