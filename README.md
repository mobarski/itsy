# Intro

## Idea

Minimalistic fantasy console API

# API

## screen

init(w, h, fps, [colors])

camera([x], [y])

* cls([col])

color(col)

pal(col1, col2)

* pset(x, y, [col])

rect(x, y, w, h, [col])

blt(x, y, bank, u, v, w, h, [colkey])

? line


## input

